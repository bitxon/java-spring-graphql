spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    hikari:
      schema: postgres
  flyway:
    default-schema: postgres
    fail-on-missing-locations: true
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: validate
  graphql:
    graphiql:
      enabled: true
